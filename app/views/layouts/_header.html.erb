<nav class="navbar navbar-default" id="navbar">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to image_tag("Logo2.png", alt: "home"), root_path %>
        </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            </button><a  class="cat" href="catalogo"><%= image_tag 'Cat.png' %>Catálogo</a>
            </ul>
            <form class="col-lg-6" id="searchf">
                <div class="form-group">
                     <input class="form-control" type="text">
                    <button type="submit" class="btn btn-default">Procurar</button>
                </div>

            </form>
            
            <ul class="nav navbar-nav navbar-right">
                <% if logged_in? %>
                    <%if current_user.usertype_id == 3 %>
                        </button> <a class="adm" href="/controll/painel"><%= image_tag 'painel.png' %>Painel ADM</a>
                    <% end %>
                <% end %>
                <% if logged_in? %>
                    </button><a class="person" href="/logout"><%= image_tag 'person.png' %>Logout</a>
                <% else %>
                    </button><a class="person" href="/login"><%= image_tag 'person.png' %>Login</a>
                <%end%>
                </button> <a class="carin" href="/cart/cartpage"><%= image_tag 'carin.png' %>
                Meu Carinho</a>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>